spring:
  application:
    name: temanlansia-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_teman-lansia
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true

server:
  port: 9000

app:
  jwt:
    # Ganti secret untuk produksi!
    secret: 177cktj1e2/dLgckDyzIF8GoYubng3KHksAbU5rl3y8=
    expiration-ms: 86400000
